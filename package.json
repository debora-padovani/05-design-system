{
    "name": "@test-ignite-ds/global",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "turbo run dev --parallel",
        "build": "turbo run build",
        "changeset": "changeset",
        "version-packages": "changeset version",
        "release": "turbo run build --filter=@test-ignite-ds/docs && changeset publish"
    },
    "devDependencies": {
        "@changesets/cli": "^2.28.1",
        "turbo": "^2.0.10"
    },
    "packageManager": "npm@10.7.0"
}
